<section>
  <div>
    <h2>Your Posts</h2>
      {{#each userData as |mypost|}}
        {{> mypost}}
      {{/each}}
  </div>
  <button href="/new">Add Post</button>

</section>


<button type="submit" class="btn"><a class="link" href="/createpost">+ New Post</a></button>

<script src="/js/create-post.js"></script>